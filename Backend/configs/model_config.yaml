model:
  type: lightgbm
  num_leaves: 255
  learning_rate: 0.05
  num_iterations: 1000
  early_stopping_rounds: 50

audio:
  sample_rate: 22050
  segment_duration: 3.0
  overlap: 1.5
  embedding_dim: 1024